{
  "scripts": {
    "babel-funcs": "babel 'src/servicesES7' --out-dir 'src/services' --presets=es2017 --copy-files --ignore 'node_modules'",
    "deploy": "yarn run babel-funcs && sls deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.152.0",
    "moment": "^2.19.2",
    "moves": "^0.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "6.17.0",
    "babel-preset-react-native": "4.0.0",    
    "babel-preset-es2017": "^6.24.1",
    "jest": "16.0.2",
    "serverless-offline": "^3.16.0"
  }
}
