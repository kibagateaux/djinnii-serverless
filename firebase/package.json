{
  "name": "node-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "compile-firebase-functions": "babel 'functionsES8' --out-dir 'functions' --presets=es2015 --copy-files --ignore 'node_modules'",
    "precompile-firebase-functions": "rimraf functions",
    "postcompile-firebase-functions": "cd functions && yarn",
    "fb-push": "yarn run compile-firebase-functions && firebase deploy",
    "host-firebase": "yarn run compile-firebase-functions && firebase serve --only functions,hosting"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.16.2",
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "babel-register": "^6.23.0",
    "express": "^4.15.2",
    "firebase-admin": "^5.1.0",
    "firebase-functions": "^0.6.1",
    "https": "^1.0.0",
    "moves": "^0.0.2",
    "rimraf": "^2.6.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1"
  }
}
