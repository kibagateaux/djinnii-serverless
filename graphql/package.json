{
  "scripts": {
    "compile": "rm -rf src/ && babel 'srcES7/' --out-dir 'src/' --presets=react-native --copy-files --ignore 'node_modules'",
    "deploy": "yarn run babel-funcs && sls deploy"
  },
  "dependencies": {
    "aws-serverless-express": "^3.0.2",
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "graphql-server-express": "^1.3.0",
    "graphql-tools": "^2.13.0",
    "neo4j-driver": "^1.5.1",
    "neo4j-graphql-js": "^0.1.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2017": "^6.24.1",
    "babel-preset-react-native": "^4.0.0",
    "serverless-offline": "^3.16.0"
  }
}
